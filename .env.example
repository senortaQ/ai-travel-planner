# ===============================================
# 🚀 AI 旅行规划师 - 环境变量配置模板
# ===============================================
# 使用说明：请复制此文件，重命名为 `.env`，并填入您的实际密钥。
# ⚠️ 注意：`.env` 文件已被 `.gitignore` 忽略，不会被提交到 GitHub。

# --- 1. Supabase 配置 (前端必须) ---
# 获取位置：Supabase Dashboard -> Settings -> API
# 请使用 "Anon Public" Key。
VITE_SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL_HERE"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_PUBLIC_KEY_HERE"

# --- 2. 高德地图配置 (前端必须) ---
# 获取位置：高德地图开放平台 -> 控制台 -> 应用管理 -> 我的应用 -> [您的应用名称]
VITE_AMAP_KEY="YOUR_AMAP_WEB_JS_API_KEY_HERE"
VITE_AMAP_SECURITY_SECRET="YOUR_AMAP_SECURITY_JSCODE_HERE"

# --- 3. AI 服务配置 (后端/Edge Function - 仅作提示) ---
# ⚠️ 注意：此密钥**不应**直接填入 `.env` 文件给前端使用！
# 您需要将您的 AI 密钥配置到 Supabase Vault 中，供后端 Edge Function 安全调用。
